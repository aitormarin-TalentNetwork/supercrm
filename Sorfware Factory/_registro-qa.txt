# Registro de rondas de QA — fichero de SOLO ANEXAR (mismas reglas que _registro-agentes.txt).
# La entrada más reciente es la vigente. NUNCA edites ni borres líneas viejas.
#
# ⚠️ DOS REGLAS QUE HAY QUE LEER ANTES DE ESCRIBIR AQUÍ (decisión 13, 2026-09-08):
#
# 1. ESCRITOR ÚNICO, DESDE LA RAÍZ. Este fichero se escribe SOLO desde el checkout de la
#    raíz del proyecto, directamente sobre `main` — nunca desde un worktree ni desde una
#    rama de tarea. Un fichero de solo-anexar que crece por el final es material clásico
#    de conflicto en cada merge; manteniéndolo fuera del flujo de ramas, el conflicto no
#    se mitiga: no puede existir. El escritor designado es el QA. Si una terminal de
#    worktree cree que necesita anexar una línea, NO lo hace: se lo pide al QA.
#
# 2. REFERENCIAS POR DEFECTO, NO CONTENIDO. Anota identificadores, no datos: id de cliente
#    en vez de nombre y teléfono, "importe fuera de rango" en vez de la cifra, id de
#    oportunidad en vez de su descripción.
#
#    EXCEPCIÓN, porque a veces el contenido ES el fallo (un nombre con un carácter que
#    rompe el render, un email con mayúsculas que no empareja, un importe con un separador
#    raro): el literal se incluye SOLO cuando es el objeto del hallazgo — el criterio es
#    "sin él, el hallazgo no es reproducible", no "es más cómodo". Y va precedido del
#    token fijo [LITERAL], siempre escrito igual, para que sea grepeable: el día del salto
#    a datos reales, el checklist corre `grep -n "\[LITERAL\]"` y revisa solo esas líneas
#    en vez de releer el registro entero a ojo.
#
#    Motivo: el QA prueba contra la app de Railway, que apunta
#    al Convex de PRODUCCIÓN. Hoy ahí solo hay datos de prueba, pero el día que este CRM
#    lleve clientes reales, este fichero pasaría a contener datos personales — y está
#    versionado en git, así que se quedarían en el historial para siempre aunque luego se
#    borren. Sacar PII de un historial de git es una operación fea que nadie debería tener
#    que hacer aquí. Cuesta lo mismo escribirlo bien hoy.
#
# Formato: timestamp | sesión | build/commit probado | alcance de la ronda | hallazgos (ids) | qué NO se pudo verificar
#
# El ÚLTIMO campo es OBLIGATORIO, no decorativo — es §2ter(b) del README aplicado al rol
# donde nació: un "no verificado" explícito es un resultado válido y valioso; el silencio
# que se lee como "verificado" es el fallo real. Caso que lo demuestra (2026-09-04): el QA
# declaró que no podía comprobar desde el navegador si `requireOwner` rechazaba de verdad
# a un `sales` en el servidor; por declararlo, el CEO lo verificó leyendo el código. Si lo
# hubiera callado, una posible brecha de permisos habría quedado sin detectar.
#
# Si no pudiste verificar nada de lo que ibas a probar, la línea se escribe igual: una
# ronda que no se pudo hacer es información, no un hueco.
#
# Creado 2026-09-08 (decisión del Factory Architect, ejecutada por el CEO) para cerrar un
# hueco real: qa.md no decía dónde anotar una ronda, así que el histórico entero del QA
# anterior (crm-curso-vibe-coding-fa) murió con su sesión y no quedó nada en disco.
2026-09-08 17:15:00 | crm-curso-vibe-coding-93 | NO DETERMINADO (desde el navegador no se ve el commit que sirve Railway; local main=235424b, sin confirmar que sea lo desplegado) | Ronda de base solo-lectura como Marta (owner): /login, /panel, /pipeline, /clientes, ficha de cliente, detalle de oportunidad, /supervision, /ajustes, modal Alta rapida (abierto y cancelado sin enviar), 404 de ruta inexistente. Consola limpia salvo el 404 del propio recurso. Mas encargo del PM sobre edicion de clientes para el alcance de la Ola 2. Sin escrituras. | Faltan los 2 registros fijos [QA] en produccion (reportado al PM, pendiente de decision de Aitor); confirmado que NO existe via en la app para editar un cliente ni corregir su email, email solo escribible en Alta rapida y etiquetado "(opcional)" (input para alcance Ola 2); etiqueta "Nueva oportunidad" duplicada en ficha de cliente = sintoma de AIT-74, no issue nueva (la Directora encontro despues un tercer caso en app/hoy/page.tsx:465); baseline de AIT-76 capturado: hoy sale el 404 ingles de fabrica incrustado dentro del shell autenticado con la barra lateral completa | (a) el commit desplegado en Railway; (b) si el detalle de una oportunidad ABIERTA ofrece controles que no vi — la unica que hay esta cerrada (Perdida), causado por el hallazgo de los registros fijos que faltan; (c) sesion de Carlos (sales) y gating por rol, incluido si un sales ve clientes ajenos (lo pidio el PM) — NO HECHO en esta ronda; (d) /hoy, /reactivar, /catalogo — NO HECHO; (e) smoke-test npm run test:e2e — NO HECHO, y ojo: corre contra localhost:3000 + Convex de dev third-goldfinch-805, NO contra el deploy de Railway; (f) si el modelo de permisos se cumple en el backend: desde el navegador no se puede distinguir filtrado real en la query de simple ocultacion en la UI
2026-09-08 17:20:00 | crm-curso-vibe-coding-93 | origin/main=235424b (verificado por git, NO por la app: sigue sin haber forma de leer desde el navegador que commit sirve Railway) | Intento de ronda REACTIVA de AIT-76 (404 propia) disparado por un aviso del CEO de que se habia publicado. Solo-lectura: ruta inexistente nueva con cache-buster, comprobacion de cabeceras de cache. | NINGUNO. La 404 seguia siendo la de fabrica en ingles, identica al baseline de las 20:10, pero NO era un fallo del producto: AIT-76 no estaba publicada. El CEO habia repetido una frase de otro rol sobre "la publicacion de hoy" (era el push del PRD) sin comprobar a que publicacion se referia. Verificado por mi con git antes de darlo por bueno: origin/main=235424b y no existen app/not-found.tsx ni app/error.tsx en origin/main. No se reporto nada al PM, a proposito: desde el navegador no se podia distinguir "deploy no vivo" de "deploy vivo y roto", asi que se pregunto primero al Integrador en vez de llamarlo hallazgo. | EL PUNTO CIEGO DEL COMMIT DESPLEGADO YA HA CAUSADO UN ERROR REAL, no es teorico: no hay buildId expuesto ni cabecera en la app que diga que commit sirve Railway (comprobado: cache-control no-store, server railway-hikari, sin buildId en el HTML), asi que el QA no puede distinguir por si mismo una app sin actualizar de una actualizada y rota, y hoy eso costo una consulta innecesaria al Integrador y una hipotesis de hallazgo que no existia. Mitigado solo para rondas REACTIVAS (el Integrador se comprometio a incluir el commit en su aviso de deploy verificado); en las rondas PERIODICAS de respaldo el hueco sigue abierto y el campo build seguira diciendo NO DETERMINADO. Ademas siguen sin hacer: sesion de Carlos (sales) y gating por rol, /hoy, /reactivar, /catalogo, y el smoke-test.
2026-09-08 17:24:00 | crm-curso-vibe-coding-93 | 235424b (origin/main — esperado, no confirmado en el servidor) | CORRECCION DE CONVENCION, no es una ronda nueva: a partir de ahora el campo build NUNCA dice "NO DETERMINADO". Se rellena con el SHA de `git ls-remote origin refs/heads/main` (comprobado por mi: devuelve 235424b4cd87b09312d9fd9a14ec802088741c33 sin permisos especiales, lo puede correr cualquiera) mas la coletilla "esperado, no confirmado en el servidor", porque Railway construye en cada push a main pero nadie puede leer desde la app que commit sirve de verdad. Decision del Factory Architect. | Retroactivo: la ronda de las 17:15 de hoy decia "NO DETERMINADO" en ese campo; su build correcto segun esta convencion es el mismo 235424b (origin/main no se movio en toda la tarde). No se edita aquella linea, se corrige aqui, que es como funciona un fichero de solo-anexar. | Sigue sin poder confirmarse el commit REAL servido por Railway: la coletilla "esperado" no es adorno, es el limite. Cuando la diferencia entre esperado y real importe para un hallazgo concreto, preguntar al Integrador (usa `railway logs --build` en su verificacion) — solo en ese caso, no en cada ronda. El arreglo de fondo (exponer NEXT_PUBLIC_COMMIT_SHA en la app) lo decidio el Factory Architect y lo lleva el PM como alcance de producto; no es del QA enrutarlo.
