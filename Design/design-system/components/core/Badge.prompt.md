Etiqueta semántica corta para feedback genérico (no estados de CRM — para eso usa `StatusBadge`).

```jsx
<Badge variant="success" dot>Conectado</Badge>
<Badge variant="info">Nuevo</Badge>
```

Variantes: `neutral | primary | success | warning | error | info`. Prop `dot` para el punto de color.
