Estado vacío amable con llamada a la acción — clave para "valor desde el primer día".

```jsx
<EmptyState icon={<i data-lucide="users" />} title="Aún no hay contactos"
  description="Añade tu primer cliente para empezar a seguir ventas."
  action={<Button leftIcon={<i data-lucide="plus" />}>Nuevo contacto</Button>} />
```
